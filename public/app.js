const $=s=>document.querySelector(s), input=$('#username'), find=$('#find'), card=$('#profile'), msg=$('#msg'), result=$('#result');
find.onclick=async()=>{const username=input.value.trim();if(!username){msg.textContent='Masukkan username Roblox terlebih dahulu.';return}msg.textContent='';find.disabled=true;card.classList.remove('hidden');result.classList.add('hidden');card.querySelector('.loading').classList.remove('hidden');try{const r=await fetch('/api/roblox/user',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username})});const d=await r.json();if(!r.ok)throw new Error(d.error);result.innerHTML=`<div class="user"><img src="${d.avatar||''}" alt="Avatar"><div><h2>${escapeHtml(d.displayName)}</h2><div class="muted">@${escapeHtml(d.username)} · ID ${d.id}</div></div></div><button class="recover" onclick="recovery('${encodeURIComponent(d.username)}')">Recover This Account</button>`;result.classList.remove('hidden')}catch(e){result.innerHTML=`<p class="muted">${escapeHtml(e.message)}</p>`;result.classList.remove('hidden')}finally{card.querySelector('.loading').classList.add('hidden');find.disabled=false}};
function recovery(u){const name=decodeURIComponent(u);result.innerHTML=`<h2>Recovery Request</h2><p class="muted">Akun: <b>@${escapeHtml(name)}</b></p><p class="muted">Untuk keamanan, jangan masukkan password, cookie .ROBLOSECURITY, atau kode OTP. Gunakan halaman bantuan resmi Roblox untuk proses pemulihan.</p><button class="recover" onclick="window.open('https://www.roblox.com/support','_blank')">Open Roblox Support</button>`}function escapeHtml(s){return String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
